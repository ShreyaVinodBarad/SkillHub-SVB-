let message: string;

function sendMessage(msg: string) {
    message = msg;
    console.log(message.toLowerCase());
}

// sendMessage(100);

sendMessage("100");

// console.log(message.toLowerCase());
